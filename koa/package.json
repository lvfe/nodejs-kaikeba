{
  "name": "koa-02",
  "version": "1.0.0",
  "description": "why use koa\r ##原生不足\r 令人困惑的api\r http.creatServer((req, resp)=>{\r     resp.setHeader(); // because it's a stream,理论性api,需要浅层封装\r     resp.end();\r })\r 应用型需要描述业务逻辑，bl\r 顺序描述需要\r 切面描述需要：日志，处理前处理后，出错， 鉴权，事务边界(装饰器可以劫持，很榕溪实现aop), reducx(applymiddleware), router guard, axios\r 语言级或者框架级-koa",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "koa": "^2.13.0",
    "koa-session": "^6.0.0"
  }
}
